{
  "name": "@stoolap/mcp",
  "version": "0.4.1",
  "description": "MCP server for Stoolap embedded SQL database",
  "type": "module",
  "bin": {
    "stoolap-mcp": "build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "stoolap",
    "sql",
    "database",
    "model-context-protocol",
    "claude",
    "ai"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stoolap/stoolap-mcp.git"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@stoolap/node": "^0.4.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
